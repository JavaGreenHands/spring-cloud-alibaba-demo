spring:
  application:
    name: nacos-config-simple
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        shared-dataids: base-common.properties,common.properties
        refreshable-dataids: common.properties